{{- $pages := where .Site.RegularPages ".Params.search" "!=" false -}}
[
{{- range $i, $p := $pages -}}
  {{- if $i }},{{ end }}
  {
    "title": {{ $p.Title | jsonify }},
    "summary": {{ $p.Summary | plainify | jsonify }},
    "uri": {{ $p.RelPermalink | jsonify }}
  }
{{- end -}}
]
